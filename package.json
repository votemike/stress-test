{
  "name": "stress-test",
  "homepage": "https://votemike.github.io/stress-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@votemike/property": "^0.2.0",
    "bulma": "^0.8.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "node-sass": "^4.13.0"
  }
}
